export enum GatewayEventsEnum {
  ORDER = 'order'
}

export enum GatewayPathsEnum {
  // order
  COMPUTE_DISCOUNT = 'COMPUTE_DISCOUNT'
}

export enum AppDomainNamesEnum {
  ORDERS = 'orders',
  USERS = 'users',
  SEED = 'seed'
}
